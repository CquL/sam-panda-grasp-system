# Model Files

The original local project used the SAM checkpoint file below:

- `sam_vit_b_01ec64.pth`

It was omitted from this GitHub export because the file is larger than GitHub's regular push limit.

Place the checkpoint in this directory with the exact filename above, or set:

`SAM_CHECKPOINT_PATH=/absolute/path/to/sam_vit_b_01ec64.pth`

